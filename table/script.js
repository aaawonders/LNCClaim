$(document).ready(function () {

    StatusStyle();

  });


function StatusStyle(){

    var claims = $(".Result");

    // claims.array.forEach(element => {
    //     var classes = element.className;
    //     console.log(classes);
    // });

    $(".Result").each(function (indexInArray, valueOfElement) { 
        console.log(indexInArray);
        console.log(valueOfElement.);
    });

    // console.log(claims);

}